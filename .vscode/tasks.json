{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nimble test",
            "type": "shell",
            "command": "nimble test",
            "problemMatcher": [
                "$nim"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}